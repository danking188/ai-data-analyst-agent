# DataTrace 项目状态与发布进程

最后更新：2026-09-16
当前阶段：阿里云小规模试点已上线（CONDITIONAL GO）
网站地址：[DataTrace 在线分析工作台](https://8.222.221.236.sslip.io/)
总体结论：注册、项目隔离、上传到报告、真实 Agent、浏览器和小流量访问均已完成线上验收；
长期运维与更大规模放量仍有待办。

版本控制快照：当前线上源码、部署脚本和本文件在本次发布中统一同步；GitHub 版本以本文件
所在提交为准。更广泛的正式生产候选仍需绿色 CI/安全门禁、签名镜像摘要和环境签署。

## 当前线上验收

2026-09-15 完整复查：公网 `/api/v1/health/ready` 返回 `ready`，SQLite 与本地对象存储
均为 `ok`；公开注册、账号隔离、退出重登、上传、质量扫描、真实建模、报告下载再次通过。
真实 Qwen Agent 完成证据解读、只读问答、计划确认边界及确认后的真实建模，3 个轮次、
5 次工具调用全部成功。Playwright 完成桌面和 390px 移动端流程，无页面异常或 5xx。

2026-09-16 Agent P0 评测强化完成：五类固定数据集的 50 个真实 Qwen 场景全部通过，
任务、任务规则和硬规则通过率均为 100%，最终 P95 29.960 秒，未授权写操作为 0。
报告保留 50 例完整基线和仅涉及因果边界的两例范围化复测链路。新增真实模型评测器、
失败分类、项目级轨迹读取与无副作用回放校验，修正证据/模型/规划路由，并加入意图和
回答结构化输出重试、有界上下文及引用失败时的确定性证据降级。

2026-09-11 完整验收：真实注册账号自动登录，创建私有项目，退出后可重新登录；管理员的
项目列表不包含新账号项目。整台服务器重启后账号和项目保留，容器、HTTPS、巡检和备份定时器
自动恢复。新账号执行上传、质量扫描、分析与报告下载成功，产生 10 个制品、2 条验证通过的
结论及 4,159 字节 HTML 报告。重名注册返回 409，弱密码返回 422。

服务器侧最新 500 次读取、并发 8：0 失败、54.740 RPS、P95 211.184 ms。该探针只覆盖
健康、就绪、会话和能力读取，不代表 8 个并发分析任务或大文件负载已验收。LLM 已通过
阿里云百炼 OpenAI-compatible 接口启用，模型为 `qwen3.8-flash`。

完整证据与试点条件见[小流量验收记录](../quality/SMALL_TRAFFIC_ACCEPTANCE.md)。

## 状态标记

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成并有验证证据
- `[!]` 需要外部环境、负责人或审批

每完成一个工作项，必须同时更新本文件中的状态、验证证据和末尾变更记录。

## 当前执行顺序

| 顺序 | ID | 状态 | 工作项 | 完成证据 |
| --- | --- | --- | --- | --- |
| 1 | REL-001 | [x] | 生产认证、CSRF、Host、Cookie 和安全响应头加固 | 后端安全/契约测试通过 |
| 2 | REL-002 | [x] | API、迁移、Worker 分离及资源限制 | Compose 配置校验通过 |
| 3 | REL-003 | [x] | 存储写入补偿、过期任务恢复和结构化日志 | 单元/集成测试通过 |
| 4 | REL-004 | [x] | 项目/版本切换、游标分页、版本比较和受保护下载闭环 | 前端单测与真实浏览器 Smoke 通过 |
| 5 | REL-005 | [x] | 锁文件、CI、依赖审计、容器扫描和 Dependabot | CI/Security 工作流已配置；本地审计无已知漏洞 |
| 6 | REL-006 | [x] | 备份、恢复、S3 版本控制及离线 LLM 发布门禁 | 脚本语法与 LLM 控制套件通过 |
| 7 | CLEAN-001 | [x] | 清理无引用代码、过时开发提示和本地生成产物 | 引用扫描通过；可再生目录已删除；CI 卫生门禁已建立 |
| 8 | REL-007 | [x] | 补齐环境样例、三类镜像扫描及清理后的全量回归 | 代码、测试、契约、审计、构建、Compose、脚本和卫生门禁通过 |
| 9 | REL-008 | [x] | 镜像运行时、供应链和发布流程加固 | 三镜像非 root；SBOM/provenance；Scout 0 Critical/High；容器 Smoke 通过 |
| 10 | REL-009 | [x] | 小流量验收、真实工作流 Smoke 与 PostgreSQL 备份恢复演练 | 本地候选 500 请求零失败；上传到报告闭环通过；隔离恢复到迁移头 |
| 11 | ENV-001 | [!] | 生产环境基础设施及业务签署 | `PRODUCTION_READINESS.md` 全部签署 |
| 12 | PILOT-001 | [x] | 阿里云部署、HTTPS、自启动与本机备份 | 2026-09-11 整机重启恢复及健康检查通过 |
| 13 | AUTH-001 | [x] | 公开注册与项目、分析、报告协同 | 2026-09-11 新账号完整线上流程、退出重登和持久化通过 |
| 14 | PILOT-002 | [!] | 长期运维补齐 | 异机备份、外部告警、自有域名与续费安排待确认 |
| 15 | AGENT-001 | [x] | 真实 Provider、证据解读、对话 Agent、计划确认与受控执行 | 2026-09-15 Qwen 真实流程与浏览器验收通过 |
| 16 | AGENT-002 | [x] | 真实模型评测、失败分类、轨迹和稳定降级 | 2026-09-16 固定样本 8/8，硬规则 100%，P95 21.827 秒 |
| 17 | AGENT-003 | [x] | 完整 50 例与五类专用数据集发布门禁 | 50/50，任务/硬规则 100%，最终 P95 29.960 秒，未授权写 0 |
| 18 | AGENT-004 | [x] | 项目级运行轨迹、只读回放校验与故障恢复回归 | 状态链/工具终态回放；结构异常、超时、熔断与陈旧任务恢复测试 |

## 已验证的仓库基线

| 范围 | 当前证据 |
| --- | --- |
| 后端质量 | Ruff、严格 Mypy 通过；181 个测试通过；覆盖率约 83% |
| 前端质量 | 2026-09-16：TypeScript 通过；34 个测试通过；生产构建通过 |
| API 契约 | Assistant 新增轨迹读取与只读回放接口；OpenAPI 与运行时响应一致 |
| 浏览器 | 2026-09-15 Playwright 桌面/390px 注册、建项目、上传、真实 Agent 问答和响应式导航通过；0 页面异常、0 HTTP 5xx |
| 依赖安全 | `pip-audit` 与 `pnpm audit` 无已知漏洞 |
| LLM 离线门禁 | 50 个评估场景及确定性引用、隔离、确认、配额和红队测试通过 |
| LLM 功能阶段 | Gate L0～L4 已完成；报告解读、对话 Assistant、受控 Copilot 与生产控制已实现 |
| LLM 线上评测 | 2026-09-16 五类固定数据集 50/50，任务/任务规则/硬规则 100%，最终 P95 29.960 秒，未授权写 0 |
| 部署配置 | Compose 渲染通过；后端、前端、试点三类候选镜像已成功构建 |
| 镜像供应链 | 构建附带 SBOM/provenance；三镜像 Docker Scout 均为 0 Critical / 0 High |
| 容器运行时 | 后端/试点 UID 999，前端 UID 101；前端 HTTP 200 + 安全头；试点迁移/Worker/API/readiness 通过 |
| 当前试点候选 | 显式认证 fail-closed、部署 Smoke、真实 Agent 完整工作流、注册用户工作流、浏览器与 500 请求小流量测试通过 |

最新功能回归（2026-09-16）：后端 181 项、前端 34 项测试全部通过，后端覆盖率约 83%，Ruff、
严格 Mypy、TypeScript、OpenAPI、自检脚本和生产构建通过。未重跑镜像扫描或供应链签名；
下述历史结果保留各自日期。

历史仓库回归（2026-08-10）：后端 148 个测试、82.61% 覆盖率，前端 26 个测试，
OpenAPI 50/63/398 校验、生成类型一致性、LLM 离线门禁、Python/前端生产依赖审计、
生产构建、Compose 渲染、Workflow YAML、Shell 语法和仓库卫生检查全部通过。

文档一致性与工作区候选回归（2026-09-08）：确认 Gate L0～L4 的计划状态、代码能力与
文档口径一致；后端 151 个测试、82.78% 覆盖率、Ruff 和严格 Mypy 通过，前端 26 个测试、
TypeScript 和生产构建通过，OpenAPI 50/63/398、LLM 50 例、依赖审计、Compose、脚本和仓库卫生检查
均通过。当前单容器试点镜像重建后完成显式认证 fail-closed、安全头 Smoke、120 行 CSV 真实
工作流、500 请求/并发 8 的小流量测试（0 失败，最终加固后 P95 9.738 ms）。PostgreSQL 16 演练
完成全量迁移、备份校验与隔离恢复，恢复库处于 `20260713_0002` 且包含 28 张 public 表。

最后一次镜像回归（2026-08-11）：三类 `linux/arm64` 镜像均成功构建并附带
SBOM/provenance；移除运行时 `uv/uvx` 后，后端和试点包数从 662 降为 154；
前端升级到 `nginx:1.30.4-alpine-slim` 后从 68 包降为 21 包。三镜像无可修复
Critical/High 漏洞，非 root 运行、前端 HTTP/安全头和试点 fail-closed/readiness 验证通过。

已知非阻断技术债：本机 Python 3.14 测试环境会从 FastAPI/Starlette 的 TestClient
兼容层发出一条 `httpx` 弃用警告；项目自身已移除 `RefResolver` 弃用调用，CI 仍以
Python 3.12 为目标。后续依赖升级时应迁移至上游推荐的 TestClient 依赖。

## 生产环境待签署

以下事项用于长期生产与更大规模放量签署；当前阿里云小规模试点的有条件可用状态和运维
待办见上文。这些事项尚未全部关闭，不构成高可用或 24×7 SLA 承诺。

| ID | 状态 | 事项 | 责任角色 | 必需证据 |
| --- | --- | --- | --- | --- |
| ENV-101 | [!] | DNS、TLS、HTTPS 跳转、WAF、限流和 DDoS 防护 | Platform/Security | 公网探测与 TLS/WAF 报告 |
| ENV-102 | [!] | Secret Manager、密钥轮换和最小权限 | Security | 配置引用与轮换演练记录 |
| ENV-103 | [!] | PostgreSQL TLS、PITR、容量及恢复演练 | DBA/QA | 本地 PostgreSQL 16 隔离恢复已通过；待目标 TLS/PITR/容量与恢复 Smoke |
| ENV-104 | [!] | 私有对象存储、加密、版本控制和生命周期 | Platform | 版本控制检查与对象恢复证据 |
| ENV-105 | [!] | 集中日志、指标、告警接收人和合成探针 | SRE | 告警触发/恢复记录 |
| ENV-106 | [!] | 100 MiB 代表文件、Worker 并发和长稳测试 | QA/SRE | 本地 500 请求/并发 8 已达标；待目标大文件、Worker 并发、长稳和资源曲线 |
| ENV-107 | [!] | 数据分级、保留/删除、隐私及供应商合规 | Legal/Security | 已批准策略或 DPA |
| ENV-108 | [!] | 正式账号模式和 LLM Canary/回滚 | Product/AI/Security | 决策记录、Canary 与回滚记录 |
| ENV-109 | [!] | 镜像构建、扫描、摘要和迁移演练 | Release Manager | 本地预检已通过；待 CI SHA、签名镜像摘要、SBOM/provenance 和目标环境发布记录 |

详细的 Go/No-Go 条件和负责人签署矩阵见
[`../deployment/PRODUCTION_READINESS.md`](../deployment/PRODUCTION_READINESS.md)。
截至 2026-09-09，ModelScope Studio 项目页可访问，但应用域名返回 HTTP 403，API 专用域名在无有效
ModelScope Token 时返回 HTTP 401；该状态不构成面向小规模用户的可用上线证据。
截至 2026-09-10，已通过本机钥匙串中的现有 ModelScope 凭据完成 OpenAPI 审计：Studio 为
`public` Docker、状态 `Sleeping`、免费 `platform/2v-cpu-16g-mem`，账号当前未返回可用付费
常驻硬件；远端 `master` 仍为 `5912af2fa07f82270458ed9454686f241a2ba2f1`。详见
[`../deployment/MODELSCOPE_LONG_RUNNING.md`](../deployment/MODELSCOPE_LONG_RUNNING.md)。
上述为 ModelScope 历史快照；当前对外入口已迁至阿里云，不能据此判定阿里云网站不可用。

## 本轮清理判定

### 删除

- 无任何 import 或路由引用的 `WorkflowPlaceholder.tsx`。
- 仅适用于早期双会话开发方式的前后端 Session Prompt。
- 依赖开放注册、已被现行部署 Smoke 与备份恢复流程替代的 `persistence_smoke.sh`。
- 可再生的覆盖率、类型检查、测试、Python 字节码、前端构建和浏览器临时产物。

### 保留

- 两份根目录 CSV：用户数据，不作为遗留物处理。
- `.env.production.local`：可能包含本地私密配置，保持忽略且不读取、不删除。
- `backend/.venv` 与 `frontend/node_modules`：当前验证所需依赖环境。
- 根 `Dockerfile` 与 `app.production`：仍是受控单容器试点入口。
- PRD、PDF、逐页渲染图和界面概念图：有明确文档或设计用途。

## 变更记录

- 2026-08-10：建立当前项目状态台账；仓库基线记录完成。
- 2026-08-10：删除无引用占位组件、早期 Session Prompt、旧持久化 Smoke，以及本地缓存/构建产物；建立 CI 仓库卫生门禁；开始清理后回归。
- 2026-08-10：修复干净克隆缺少前后端 `.env.example` 的问题；安全工作流覆盖 API、前端 Nginx 和单容器试点三个镜像；补充单容器试点的 fail-closed 配置说明。
- 2026-08-10：移除契约测试对已弃用 `jsonschema.RefResolver` 的依赖，改用 OpenAPI 3.1 对应的 Draft 2020-12 校验器。
- 2026-08-10：修正 Python 审计流程，不再尝试构建本地可编辑项目或临时升级 pip；当前 Python 与前端生产依赖复审均无已知漏洞。
- 2026-08-10：清理后全量回归完成；REL-007 关闭；测试生成的缓存和构建产物再次清除，仓库进入候选发布状态。
- 2026-08-11：三类候选镜像完成本地构建、SBOM/provenance、非 root 和真实容器 Smoke 验证。
- 2026-08-11：根据镜像扫描移除运行时 `uv/uvx`，将前端基础镜像升级为 Nginx 1.30.4 Alpine Slim；三镜像复扫均为 0 Critical / 0 High。
- 2026-08-11：新增标签发布流程，只在 CI/安全门禁通过后发布多架构镜像，附带 SBOM/provenance、Sigstore 签名和发布证据清单。
- 2026-09-08：统一 README、契约变更记录和开发计划中的 LLM 状态；补记 Gate L0～L4 已完成及最新本地回归基线，保持生产环境签署为 No-Go 条件。
- 2026-09-09：新增安全部署 Smoke、小流量探针与可回收的真实工作流验收；工作区候选通过 500 请求/并发 8、完整分析报告闭环和 PostgreSQL 16 备份恢复演练。
- 2026-09-10：根据 ModelScope 官方规则与 OpenAPI 补充长期运行手册；确认当前公开 Docker Studio 处于免费资源休眠状态、远端版本过旧，免费 Studio 不构成 24×7 SLA。
- 2026-09-11：阿里云公开注册上线；完成账号与项目隔离、上传到报告、退出重登、整机重启和 500/8 读取验收；前端测试增至 28 项。
- 2026-09-14：复查线上 readiness 与注册开关正常；同步 GitHub 网站入口、项目状态和发布证据，保留运维待办及浏览器验收限制。
- 2026-09-15：启用百炼 `qwen3.8-flash`；完成真实 Agent 端到端、注册用户完整流程、
  500 请求/并发 8 和桌面/移动端浏览器验收；修复叙事数字符号纠错、非法计划纠错、目标列
  排除归一化、状态中文化和 Agent 指标刷新，并统一线上、代码与文档进度。
- 2026-09-16：完成五类数据集准备器与真实 Provider 50 例发布门禁；补齐报告导出、范围过滤、
  结构化回答重试、确定性因果边界、项目级运行轨迹和无副作用回放校验，并加入故障注入回归。
