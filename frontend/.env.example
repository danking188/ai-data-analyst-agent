VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_API_MODE=mock
# Only set this for a local backend using AUTH_MODE=dev_token. Production builds never accept it.
VITE_DEV_AUTH_TOKEN=
VITE_CSRF_COOKIE_NAME=datatrace_csrf
